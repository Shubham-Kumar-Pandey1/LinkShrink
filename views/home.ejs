<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL SHORTENER</title>
    <style>
        /* Global styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7f8fc;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        h1 {
            color: #4e73df;
            font-size: 36px;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        /* Container for centering the content */
        .container {
            background-color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            padding: 30px;
            width: 100%;
            max-width: 500px;
        }

        .container p {
            font-size: 16px;
            color: #555;
        }

        /* Form styling */
        label {
            font-size: 16px;
            color: #333;
            margin-bottom: 8px;
            display: block;
            text-align: left;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px;
            margin: 10px 0 20px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 16px;
            box-sizing: border-box;
            outline: none;
            transition: border-color 0.3s ease-in-out;
        }

        input[type="text"]:focus {
            border-color: #4e73df;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #4e73df;
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
        }

        button:hover {
            background-color: #2e59d9;
        }

        /* Link styling */
        .generated-url {
            margin-top: 20px;
            font-size: 16px;
            color: #4e73df;
            word-wrap: break-word;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            h1 {
                font-size: 28px;
            }

            .container {
                padding: 20px;
            }

            button {
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>URL SHORTENER</h1>
        <% if (locals.id) { %>
            <p class="generated-url">URL Generated: <a href="http://localhost:8000/<%= id %>" target="_blank">http://localhost:8000/<%= id %></a></p>
        <% } %>   
        <form action="/" method="POST">
            <label for="url">Enter your Link:</label>
            <input type="text" name="url" id="url" placeholder="https://www.example.com" required>
            <button type="submit">Generate</button>
        </form>
    </div>
</body>
</html>
